Mejoras:
- Hacer que se vea el estado del programa (ver que se está descargando)
- Añadir función de "imprimir" en "funciones_yt" -> imprimir en terminal o en la aplicación dependiendo si se ejecuta directamente o se importa en otro archivo.py
    - Código beta:
        def imprimir(text):
            if (__name__ == "__main__"):
                print(text)
            else:

- Botón para parar la descarga de una playlist.
- ERROR -> se descarga en la carpeta principal, no en la de descargas/root (a veces)
    - Maybe es porque no se lee el directorio de descargas cuando se cambia, o algo así.
- Error -> cambiar nombre cuando se descarga un video solo (visualizer) 