from tkinter import * # Interfaz gráfica.
#pyw -> extension para no abrir terminal
# Parte gráfica.

raiz = Tk()
raiz.title("YouTube to MP3") # Título.
raiz.iconbitmap("yt_logo.ico") # Icono.

f = Frame()
f.pack(fill = "both", expand = "True")
f.config(bg="green")
f.config(width="1200", height="675")

raiz.mainloop()
